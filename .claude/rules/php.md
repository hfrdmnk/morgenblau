---
paths:
    - '**/*.php'
---

Use the `laravel-best-practices` skill when working with Laravel PHP code. Also invoke `inertia-react-development`: Inertia spans both sides, so PHP controllers returning `Inertia::render()` need the same skill loaded as the React pages they target.
