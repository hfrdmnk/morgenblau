procs:
  Server:
    shell: air
    autostart: true
  Vite:
    shell: bun run --cwd ./frontend dev
    autostart: true
  Tunnel:
    shell: cloudflared tunnel run
    autostart: true
  Tests:
    shell: go test ./...
    autostart: false
